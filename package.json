{
  "name": "beenycool.github.io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cloudflare-build": "npm install && npx rimraf backend/.next && next build ./app && next export ./app"
  }
}